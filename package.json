{
  "name": "get-random-card",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy-dev": "node-lambda deploy --environment development",
    "deploy-prod": "node-lambda deploy --environment production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "node-lambda": "^0.7.1"
  }
}
